/**
 * Live Media Service entrypoint (SAD §2, §4.3) — WebRTC SFU (mediasoup) for
 * native live classrooms.
 *
 * DEFERRED to Phase 15 (plan D2: Zoom/Teams first, native behind a feature flag).
 * This is an intentional placeholder so the deploy topology and workspace layout
 * exist from day one without pulling in mediasoup until the phase begins.
 */
function main(): void {
  console.log('[lumora-media] placeholder — native WebRTC SFU is implemented in Phase 15.');
}

main();
